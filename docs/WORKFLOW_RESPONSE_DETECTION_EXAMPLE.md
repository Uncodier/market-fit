# Retired Workflow Response Example

The implementation example formerly stored here targeted the removed
`/api/workflows/webhook` route. It is intentionally not preserved as copyable
code because doing so would recreate an unauthenticated privileged callback.

See [the retired integration note](WORKFLOW_WEBHOOK_INTEGRATION.md) and
[the security guide](SECURITY.md) before designing a replacement.
