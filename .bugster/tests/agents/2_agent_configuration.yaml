name: Agent Configuration
page: agents/:id
page_path: app/agents/[id]/page.tsx
task: Configure an AI agent
steps:
- Navigate to agents page
- Click on a specific agent
- Toggle various capabilities (tools, integrations)
- Upload context file for the agent
- Save changes and verify they persist after page reload
expected_result: Agent configuration is saved successfully
