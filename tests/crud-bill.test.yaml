goal: Complete CRUD cycle for a Bill

statements:
  - URL: /projects
  - intent: Select the first available project
    action: click
    locator: "getByRole('button', { name: 'Select' }).first()"

  - WAIT_UNTIL: The user is navigated to the default project page
    js: "await page.url().includes('/robots')"
    timeout_seconds: 5

  - URL: /bills
  - intent: Click New bill in the top bar
    action: click
    locator: "getByRole('button', { name: 'New bill' })"

  - intent: Fill the bill title
    action: fill
    locator: "getByRole('dialog', { name: 'New vendor bill' }).getByRole('textbox').first()"
    value: "Automated Test Bill"

  - intent: Fill the line item name
    action: fill
    locator: "getByRole('dialog', { name: 'New vendor bill' }).getByRole('textbox').nth(1)"
    value: "Test Line Item"

  - intent: Submit the create bill form
    action: click
    locator: "getByRole('button', { name: 'Create bill' })"

  - description: Wait for the bill creation to complete
    js: |
      await expect(page.getByRole('dialog', { name: 'New vendor bill' })).toBeHidden({ timeout: 15000 });
      await expect(page).toHaveURL(/\/bills\/[^/]+$/, { timeout: 15000 });

  - VERIFY: The created bill title is visible
    js: "await expect(page.getByText('Automated Test Bill').first()).toBeVisible()"

  - intent: Click Delete on the bill details toolbar
    action: click
    locator: "getByRole('button', { name: 'Delete' })"

  - intent: Confirm deletion of the bill
    action: click
    locator: "getByRole('button', { name: 'Delete' }).last()"

  - WAIT_UNTIL: The user is back on the bills list
    js: "await page.url().endsWith('/bills')"
    timeout_seconds: 15

  - VERIFY: The bill is gone from the list
    js: "await expect(page.getByText('Automated Test Bill')).toHaveCount(0)"
